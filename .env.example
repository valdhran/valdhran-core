# Valdhran — variables de entorno de desarrollo
# Copiar como .env y completar con valores reales

DATABASE__URL=postgres://valdhran:valdhran_dev@localhost:5433/valdhran
DATABASE__MAX_CONNECTIONS=5

JWT__SECRET=CAMBIAR_EN_PRODUCCION
JWT__ACCESS_TOKEN_EXPIRY_SECONDS=900
JWT__REFRESH_TOKEN_EXPIRY_SECONDS=604800

SERVER__HOST=127.0.0.1
SERVER__PORT=8080
